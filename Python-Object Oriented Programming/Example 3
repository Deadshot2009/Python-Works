class Book:
    def __init__(self,title,author,price):
        self.title=title 
        self.author=author
        self.price=price
    def display(self,percent):
        print(self.title,self.author,self.price)
        new_price=price-(price*percent/100)
obj1=Book("Python","Guido",500)
obj1.display()
obj1.display(20)
obj1.display()